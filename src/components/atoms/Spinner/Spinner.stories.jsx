import React from 'react';
import { Spinner } from './Spinner';

export default {
  title: 'Ui/Atom/Spinner',
  component: Spinner,
};

const Template = () => <Spinner />;

export const Default = Template.bind({});
